{
  "name": "ofsa-connector",
  "config": {
    "tasks.max": 10,
    "connector.class": "io.confluent.connect.jdbc.JdbcSourceConnector",
    "connection.url":"jdbc:oracle:thin:@ofsa:1521/ofsa?user=stage&password=***",
    "mode":"bulk",
    "query":"SELECT /*+ parallel(10) */ * FROM dwh.REF_EMPLOYEES",
    "topic.prefix":"DWH.REF_EMPLOYEES",
    "batch.size" : "100000"
  }
}